{
    "name": "ems-copilot-frontend",
    "private": true,
    "packageManager": "npm@11.6.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "devDependencies": {
        "turbo": "^2.5.8"
    },
    "scripts": {
        "dev:paramedic": "turbo run dev --filter=paramedic",
        "dev:hospital": "turbo run dev --filter=hospital",
        "build": "turbo run build",
        "lint": "turbo run lint"
    }
}
